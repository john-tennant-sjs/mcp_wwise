# WAAPI connection settings
# Copy to .env and adjust as needed (or set via your MCP host's env config).

# TCP port that Wwise's WAAPI WebSocket server listens on.
# Matches the Wwise default; only change if your project uses a different port.
WWISE_WAAPI_PORT=8080
